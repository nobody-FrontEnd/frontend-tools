export interface jsonDataType {
    name: string;
    path: string;
}

const jsonData: jsonDataType[] = [
    {
        name: 'JSON美化',
        path: '/json/beauty',
    },
];

export default jsonData;
